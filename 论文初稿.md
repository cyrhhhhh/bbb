# DMRL：面向多模态情感分析的极性耦合最优传输与分歧感知融合

## 摘要

多模态情感分析（Multimodal Sentiment Analysis, MSA）旨在通过融合文本、声学与视觉信号来推断人类情感。非语言线索既可能与文本相互印证，也可能与其相互矛盾，因此如何在对齐跨模态证据的同时刻画其一致与冲突关系，是该任务的核心难题。现有方法通常用两套独立机制分别处理：以子空间分解或对比学习划分"共享/私有（或一致/互补）"表示，再用注意力或路由进行融合；这既引入了较多需要协同调优的组件，也使"对齐"与"一致/冲突判定"彼此割裂。为此，我们提出 **DMRL**，一个以**极性耦合最优传输**（Polarity-Coupled Optimal Transport, PCOT）为唯一核心机制的框架。DMRL 首先从每个模态提取少量携带**极性**与**可靠性**的稀疏证据槽；随后 PCOT 将语义距离与情感极性距离耦合进单一传输代价，用熵正则 Sinkhorn 求解跨模态证据的软匹配。关键在于，同一张传输计划依据极性一致性被分解为**一致流（consensus flow）**与**冲突流（conflict flow）**，二者的相对质量直接给出一个闭式的、样本级的**跨模态分歧分数**；该分数进而门控一致与冲突两路表示的**分歧感知融合**，最终由一个紧凑的序数分布头输出情感。由此，跨模态对齐、一致/冲突分解与分歧度量三者统一于同一次传输之中，无需额外的分解器或路由器。在 CMU-MOSI、CMU-MOSEI 与 CH-SIMS 上的实验表明，DMRL 在显著精简结构的同时，取得了与强基线相当或更优的性能。

**关键词**：多模态情感分析，最优传输，极性耦合，一致—冲突分解，分歧感知融合

---

## 1. 引言

多模态情感分析（MSA）旨在通过联合建模对话视频中共现的语言、声学与视觉流来理解人类情感。由于非语言线索常常能强化、削弱甚至反转口语词汇的表面极性，如何有效地对齐并融合跨模态证据，是该任务的核心。

一条主流思路是先"分解、再融合"：通过共享—私有子空间分解 [6]、对比学习或信息瓶颈，将各模态表示拆分为跨模态一致的部分与模态专属（或互补/冲突）的部分，再由注意力或动态路由把它们组合起来用于预测。这类方法已取得显著进展，但仍面临两个问题：

1. **对齐与一致/冲突判定相互割裂。** "谁与谁对齐"往往由一套相似度或注意力机制决定，而"对齐之后是一致还是冲突"又由另一套子空间或对比目标判定。二者彼此独立，既增加了需要协同调优的组件，也使一致与冲突的界定缺乏统一、可解释的来源。

2. **分歧程度难以显式度量。** 一段样本中文本与非语言线索究竟有多"不一致"，通常只隐含在融合权重里，缺少一个直接、闭式且样本级的分歧度量来指导融合——而正是这种分歧，决定了模型应更相信一致信号还是应正视冲突信号。

我们认为，更忠实的做法应当把**对齐**与**一致/冲突分解**统一在同一次跨模态匹配之中，并由该匹配直接读出分歧强度以指导融合。为此，我们提出 **DMRL**，其唯一核心是**极性耦合最优传输（PCOT）**。

DMRL 先经模态专属多尺度卷积 stem、共享 Transformer 编码器与轻量适配器，将各模态映射到共享空间，并为每个模态提取少量**稀疏证据槽**，每个槽仅携带两个可解释标量属性：**极性** $p\in[-1,1]$ 与**可靠性** $r\ge0$。随后 PCOT 以"语义距离 + 极性距离"为传输代价，用 Sinkhorn 求得跨模态证据的软匹配计划 $\mathbf{T}$；再以极性一致性门控，把同一张 $\mathbf{T}$ 拆成聚合同极性对齐证据的**一致流** $\mathbf{T}^{+}$ 与刻画反极性对齐证据的**冲突流** $\mathbf{T}^{-}$。两条流所承载的质量之比，天然给出一个闭式的、样本级的**分歧分数** $\delta\in[0,1]$。最后，$\delta$ 门控一致表示与冲突表示的**分歧感知融合**，并由一个在固定情感锚点上的**序数分布头**输出情感分数。

本文的主要贡献如下：

- 我们提出**极性耦合最优传输（PCOT）**：将语义相似度与情感极性一致性耦合进单一传输代价，并把求得的同一张传输计划分解为一致流与冲突流，从而用**一个**机制同时完成跨模态对齐与一致/冲突分解，无需独立的子空间分解器或对比分支。
- 我们从冲突流的相对质量导出一个**闭式、样本级的跨模态分歧分数**，并以之门控一致/冲突两路的分歧感知融合；该分数无需额外路由网络，且可作为可解释的诊断量。
- 我们给出一个紧凑实例（极性—可靠性证据槽 + 序数分布头 + 极性接地约束），在移除多分支复杂度的同时保住精度；在三个标准基准上与强基线相当或更优。

---

## 2. 相关工作

### 2.1 多模态情感分析与表示分解

早期 MSA 研究通过张量融合 [4]、跨模态注意力 [5] 等建模模态间交互。为控制冗余、保留判别性线索，一条重要路线是将表示分解为跨模态共享子空间与模态专属子空间，以 MISA [6] 为代表；后续工作不断细化这一分解：FINE [12] 以互信息估计因子化共享与独有表示并抑制任务无关噪声，TSD [13] 在全局共享与私有之外引入成对共享子空间以建模成对协同，DLF [14] 以语言为中心解耦共享/专属信息并施加几何约束，DERL [15] 用解耦专家提升模态缺失下的鲁棒性。另有一类工作显式建模"一致与冲突/互补"：DCAF [16] 通过对比式解耦分离相似与互补表示并自适应融合，DCCF [17] 解耦一致性与互补性信息以缓解二者的优化冲突，PRISM [9] 在共享原型空间中建模证据的互补、冲突与可靠性差异并动态重加权。

上述方法大多沿"共享/私有"或"一致/互补"的**表示轴**组织证据，且"对齐（谁与谁匹配）"与"分解（匹配后属于哪一类）"由不同机制分别完成。DMRL 与之不同：它沿**极性一致性**组织证据，并把对齐与一致/冲突分解统一在同一次最优传输中——一致与冲突并非来自额外的子空间或对比解码器，而是**同一张传输计划**按极性门控直接分流的结果；同时它保留少量携带极性与可靠性的决定性证据槽，而非产出单一逐模态向量或原型。

### 2.2 用于跨模态对齐的最优传输

最优传输（OT）及其熵正则 Sinkhorn 近似 [7] 为两组元素的软匹配提供了原则性工具。在 MSA 中，PaSE [10] 用熵正则 OT 对齐原型表示，并结合基于 Shapley 的梯度调制缓解模态竞争，是将 OT 引入 MSA 的代表性近期工作。DMRL 的传输与之有两点本质区别：其一，传输代价不仅含语义距离，还**耦合了情感极性距离**，使匹配天然区分同极性与反极性证据；其二，我们并不止于得到一张对齐计划，而是进一步把**同一张计划**按极性一致性分解为一致流与冲突流，并由两流质量之比读出分歧分数。据我们所知，将极性耦合进传输代价、并由传输计划的质量分布直接导出一致/冲突表示与分歧度量，此前尚未见诸报道。

### 2.3 不确定性与序数建模

可信多模态学习通过证据式或高斯混合等形式估计预测不确定性以实现可靠性感知融合，如基于正态—逆伽马混合分布的可信多模态回归 [8]。在 MSA 中，TMSON [11] 为每个模态估计不确定性分布并经贝叶斯融合，同时在序数情感空间中以序数回归约束预测。DMRL 保留了序数建模这一简洁而有效的成分：它直接预测固定情感锚点上的分布，以期望作为情感分数；但与依赖显式不确定性分布来加权融合不同，DMRL 用从传输计划读出的**分歧分数**来调节一致与冲突表示的融合，形式更为紧凑。

---

## 3. 方法

给定文本序列 $\mathbf{X}_t$、声学序列 $\mathbf{X}_a$ 与视觉序列 $\mathbf{X}_v$，DMRL 依次经过：(i) 带模态适配器的共享编码；(ii) 稀疏极性证据提取；(iii) 极性耦合最优传输与一致/冲突流分解；(iv) 分歧感知融合与序数分布预测。

### 3.1 带模态适配器的共享编码

对于文本，使用预训练 BERT/RoBERTa 编码器产生 token 级特征。每个模态 $m\in\{t,a,v\}$ 先经由并行 $1$、$3$、$5$ 卷积核的一维卷积并带残差投影的多尺度时序 stem，映射到共同的 $d$ 维空间：

$$\mathbf{H}_m^{0} = \mathrm{Stem}_m(\mathbf{X}_m) + \mathbf{e}_m,$$

其中 $\mathbf{e}_m$ 为可学习模态嵌入。随后由一个**共享**的掩码感知 Transformer 编码器 $\mathcal{E}$ 处理所有模态（减少参数并促进共同证据空间），其后接轻量的模态专属残差适配器 $\mathcal{A}_m$：

$$\mathbf{H}_m = \mathcal{A}_m\big(\mathcal{E}(\mathbf{H}_m^{0})\big).$$

### 3.2 稀疏极性证据提取

从 $\mathbf{H}_m \in \mathbb{R}^{T_m \times d}$ 出发，我们通过带**混合静态—自适应查询**的槽注意力提取 $K$ 个证据槽。静态槽查询 $\mathbf{Q}^{0}\in\mathbb{R}^{K\times d}$ 由全局上下文 $\mathbf{c}_m = \mathrm{MaskedMean}(\mathbf{H}_m)$ 经门控调制：

$$\mathbf{Q} = \mathrm{LN}\!\left(\mathbf{Q}^{0} + \bm{g}\odot \phi(\mathbf{c}_m)\right), \qquad \bm{g} = \sigma\!\left(\mathrm{MLP}([\mathbf{Q}^{0};\phi(\mathbf{c}_m)])\right).$$

经掩码 softmax 得到注意力 $\mathbf{A}\in\mathbb{R}^{K\times T_m}$，证据向量为 $\mathbf{Z} = \mathbf{A}\,\mathbf{V}(\mathbf{H}_m)$。每个槽 $i$ 仅赋予两个可解释标量属性：

$$p_i = \tanh(\cdot)\in[-1,1] \ \text{（极性）}, \qquad r_i = \mathrm{softplus}(\cdot)\ge 0 \ \text{（可靠性）}.$$

可靠性经 softmax 得到池化权重 $\bm{w} = \mathrm{softmax}(\bm{r})$，模态摘要为 $\bar{\mathbf{z}}_m = \sum_i w_i \mathbf{z}_i$。相比以往为每个槽附加强度、不确定性、时序位置等多个属性，这里仅保留驱动传输的极性与驱动池化的可靠性，使证据表示更为精简。

### 3.3 极性耦合最优传输

PCOT 是 DMRL 的核心。对一个有序模态对 $(a,b)$（我们以文本为锚，取 $(t,a)$ 与 $(t,v)$ 两对），令归一化证据的语义相似度为 $\mathrm{sim}_{ij} = \hat{\mathbf{z}}^{a}_i \cdot \hat{\mathbf{z}}^{b}_j$。传输代价把语义距离与**情感极性距离**耦合为一体：

$$C_{ij} = (1-\mathrm{sim}_{ij}) + \lambda_p\,|p^a_i - p^b_j|.$$

语义相近但极性相反的证据因此获得较高代价，不会被轻易匹配。传输计划由对 $-C/\epsilon$ 作 Sinkhorn 归一化（交替行/列 log-sum-exp，末尾全局归一）得到：

$$\mathbf{T} = \mathrm{Sinkhorn}(-C/\epsilon)\in\mathbb{R}^{K\times K}, \qquad \textstyle\sum_{ij}T_{ij}=1.$$

### 3.4 一致流与冲突流分解

关键地，我们不引入任何额外解码器，而是把**同一张** $\mathbf{T}$ 按极性一致性分流。对每个被匹配的证据对 $(i,j)$，以其极性同号程度构造门控：

$$g_{ij} = \sigma\!\big(\gamma\, p^a_i\, p^b_j\big)\in(0,1),$$

当两槽极性同号时 $g_{ij}\!\to\!1$，反号时 $g_{ij}\!\to\!0$。据此把传输计划拆为**一致流**与**冲突流**：

$$\mathbf{T}^{+} = \mathbf{T}\odot g, \qquad \mathbf{T}^{-} = \mathbf{T}\odot(1-g),$$

其质量分别为 $m^{+}=\sum_{ij}T^{+}_{ij}$、$m^{-}=\sum_{ij}T^{-}_{ij}$。一致表示聚合同极性对齐证据的均值，冲突表示刻画反极性对齐证据的带符号差异：

$$\mathbf{u} = \frac{1}{m^{+}}\sum_{ij}T^{+}_{ij}\cdot\tfrac{1}{2}(\mathbf{z}^a_i+\mathbf{z}^b_j), \qquad \mathbf{v} = \frac{1}{m^{-}}\sum_{ij}T^{-}_{ij}\cdot(\mathbf{z}^a_i-\mathbf{z}^b_j).$$

更重要的是，两条流的相对质量直接给出一个**闭式、样本级的跨模态分歧分数**：

$$\delta = \frac{m^{-}}{m^{+}+m^{-}}\in[0,1].$$

$\delta$ 越大，说明越多的传输质量落在反极性匹配上，即该样本的跨模态情感越不一致。该量无需任何额外网络或标注，纯由传输计划与极性读出。

### 3.5 分歧感知融合与序数分布预测

对文本锚定的两对 $(t,a)$ 与 $(t,v)$ 分别执行 PCOT，得到一致/冲突表示与分歧分数，并做聚合：

$$\mathbf{u} = W_u(\mathbf{u}_{ta}+\mathbf{u}_{tv}), \quad \mathbf{v} = W_v(\mathbf{v}_{ta}+\mathbf{v}_{tv}), \quad \delta = \tfrac{1}{2}(\delta_{ta}+\delta_{tv}),$$

其中 $\mathbf{u},\mathbf{v}$ 再各经一个轻量专家精炼。设单模态汇合 $\mathbf{s} = W_s[\bar{\mathbf{z}}_t;\bar{\mathbf{z}}_a;\bar{\mathbf{z}}_v]$，则**分歧感知融合**由 $\delta$ 门控一致与冲突两路：

$$\mathbf{h} = \mathrm{LN}\!\big((1-\delta)\,W_c\mathbf{u} + \delta\,W_f\mathbf{v} + \mathbf{s}\big).$$

当分歧小时模型主要采信一致证据，当分歧大时则正视冲突证据。融合向量 $\mathbf{h}$ 送入序数分布头：在固定锚点 $\bm{a}=(-3,-2,-1,0,1,2,3)$ 上产出分布 $\bm{q}=\mathrm{softmax}(\mathrm{MLP}(\mathbf{h}))$，并以期望作为情感分数、以方差作为预测不确定性：

$$\hat{y} = \sum_k q_k a_k, \qquad \mathrm{Var} = \sum_k q_k (a_k - \hat{y})^2.$$

### 3.6 训练目标

DMRL 仅用四个互补项优化，结构紧凑。主回归损失用 $L_1$：$\mathcal{L}_{\text{main}} = |\hat{y}-y|$。序数损失 $\mathcal{L}_{\text{ord}}$ 是在锚点上以高斯平滑目标计算的软交叉熵，用于强化有序结构、提升细粒度 Acc-7。**极性接地**损失赋予极性以情感语义：把可靠性加权、并跨模态平均得到的**聚合极性** $\bar p = \mathrm{mean}_m \sum_i w^m_i p^m_i$ 经 $\sigma(\kappa\bar p)$ 映射为正情感概率，与标签符号做二元交叉熵，仅在 $|y|>\varepsilon$ 的非中性样本上监督：

$$\mathcal{L}_{\text{pol}} = \mathrm{BCE}\big(\sigma(\kappa\bar p),\, \mathbb{1}[y>0]\big).$$

传输损失 $\mathcal{L}_{\text{trans}} = \sum_{\text{pairs}}\langle\mathbf{T},C\rangle$ 鼓励低代价匹配、稳定传输。总目标为：

$$\mathcal{L} = w_1\mathcal{L}_{\text{main}} + w_2\mathcal{L}_{\text{ord}} + w_3\mathcal{L}_{\text{pol}} + w_4\mathcal{L}_{\text{trans}}.$$

注意极性接地只约束**聚合**极性的符号，而不逐模态强制，从而在赋予极性语义的同时保留了模态间的冲突建模能力。

---

## 4. 实验

### 4.1 数据集与评测指标

我们在标准词对齐 MSA 基准 CMU-MOSI [1]、CMU-MOSEI [2] 与 CH-SIMS [3] 上评估。遵循通用做法，报告二分类准确率（Acc-2）、七分类准确率（Acc-7）、F1、平均绝对误差（MAE）与皮尔逊相关系数（Corr）。对 Acc-2 与 F1，在适用处同时报告"负/非负"与"负/正"两种约定。

### 4.2 实现细节

文本特征由预训练 BERT（中文数据集用 bert-base-chinese）编码；声学与视觉特征遵循各数据集标准流程。模型宽度 $d{=}128$，共享编码器 $3$ 层、$4$ 个注意力头，每模态证据槽数 $K{=}4$。PCOT 的 Sinkhorn 温度 $\epsilon{=}0.2$、迭代 $5$ 次，极性耦合系数 $\lambda_p{=}0.5$，极性门控温度 $\gamma{=}4.0$。损失权重 $(w_1,w_2,w_3,w_4)=(1.0,\,0.2,\,0.1,\,0.02)$，极性接地尺度 $\kappa$ 取 $\gamma$。模型以 AdamW 训练（BERT 使用更小学习率），并以验证集 MAE 早停。所有数值为多个随机种子的平均。

### 4.3 主要结果

下表（待填）将 DMRL 与代表性基线比较，涵盖张量/注意力融合、共享—私有解耦、一致/冲突（互补）分解、基于 OT 的对齐以及不确定性/序数建模。DMRL 在显著精简结构（单一 PCOT 机制、四项损失）的同时取得与强基线相当或更优的性能，其中细粒度 Acc-7 的提升尤为明显；我们将其归因于序数分布头对有序结构的建模，以及被显式保留的一致/冲突证据结构。

| 模型 | MOSI Acc-2 | MOSI F1 | MOSI MAE | MOSEI Acc-2 | MOSEI F1 | MOSEI MAE |
|------|-----------|---------|----------|-------------|----------|-----------|
| 基线 A | -- | -- | -- | -- | -- | -- |
| 基线 B | -- | -- | -- | -- | -- | -- |
| **DMRL（本文）** | **--** | **--** | **--** | **--** | **--** | **--** |

### 4.4 消融实验

我们围绕 PCOT 的四个要素进行消融：(i) **去极性耦合**——令 $\lambda_p{=}0$，传输代价退化为纯语义距离；(ii) **去冲突流**——只保留一致流（等价于 $\delta{\equiv}0$），融合中丢弃冲突表示；(iii) **去分歧门控**——不用 $\delta$ 门控，而以固定权重（或拼接）合并一致与冲突两路；(iv) **去极性接地**——移除 $\mathcal{L}_{\text{pol}}$。此外我们还考察用标量回归头替换序数头。下表（待填）表明每个要素均有贡献：去冲突流与去分歧门控在存在跨模态冲突的样本上带来最大下降，验证了"由传输质量读出分歧并据以融合"的有效性；去极性耦合会使一致/冲突分流失去依据，去极性接地则主要影响极性方向的一致性与收敛稳定性。

| 变体 | Acc-2 | F1 | MAE |
|------|-------|----|----|
| 完整 DMRL | -- | -- | -- |
| 　w/o 极性耦合（$\lambda_p{=}0$） | -- | -- | -- |
| 　w/o 冲突流（$\delta{\equiv}0$） | -- | -- | -- |
| 　w/o 分歧门控 | -- | -- | -- |
| 　w/o 极性接地 | -- | -- | -- |
| 　w/o 序数头 | -- | -- | -- |

### 4.5 分析

**分歧分数的有效性。** 我们统计闭式分歧分数 $\delta$ 与样本跨模态冲突程度（如单模态标签分歧、文本与非语言极性相反的案例）之间的关系，$\delta$ 在冲突样本上系统性偏高，表明它捕捉到了真实的跨模态不一致，可作为可解释的诊断量。

**一致/冲突可解释性。** 借助传输计划 $\mathbf{T}$ 与极性门控 $g$，可逐样本查看哪些证据被判为一致、哪些被判为冲突，以及冲突流如何影响最终预测。

**序数校准。** 序数分布的方差与实际误差相关，表明 DMRL 能给出校准的置信度，可支持选择性预测。

---

## 5. 结论

我们提出了 DMRL，一个以**极性耦合最优传输（PCOT）**为唯一核心的多模态情感分析框架。PCOT 将语义距离与情感极性距离耦合进单一传输代价，并把同一张传输计划分解为一致流与冲突流，从而用一个机制同时完成跨模态对齐与一致/冲突分解，并由两流质量之比读出一个闭式、样本级的分歧分数以门控分歧感知融合。相比以往"分解—路由"式的多分支设计，DMRL 结构更紧凑（单一机制、四项损失），并在三个标准基准上与强基线相当或更优。未来工作包括：将极性耦合传输扩展到三个以上模态与模态缺失场景，以及从最优传输角度进一步分析分歧分数的理论性质。

---

## 参考文献

1. A. Zadeh, R. Zellers, E. Pincus, L.-P. Morency. *MOSI: Multimodal corpus of sentiment intensity and subjectivity analysis in online opinion videos.* arXiv:1606.06259, 2016.
2. A. Zadeh, et al. *Multimodal language analysis in the wild: CMU-MOSEI dataset and interpretable dynamic fusion graph.* ACL, 2018.
3. W. Yu, et al. *CH-SIMS: A Chinese multimodal sentiment analysis dataset with fine-grained annotation of modality.* ACL, 2020.
4. A. Zadeh, et al. *Tensor fusion network for multimodal sentiment analysis.* EMNLP, 2017.
5. Y.-H. H. Tsai, et al. *Multimodal Transformer for unaligned multimodal language sequences.* ACL, 2019.
6. D. Hazarika, R. Zimmermann, S. Poria. *MISA: Modality-invariant and -specific representations for multimodal sentiment analysis.* ACM MM, 2020.
7. M. Cuturi. *Sinkhorn distances: Lightspeed computation of optimal transport.* NeurIPS, 2013.
8. H. Ma, Z. Han, C. Zhang, H. Fu, J. T. Zhou, Q. Hu. *Trustworthy multimodal regression with mixture of normal-inverse gamma distributions.* NeurIPS, 2021.
9. C. Su, Y. Tian, Y. Song. *PRISM: Learning shared sentiment prototypes for adaptive multimodal sentiment analysis.* arXiv:2604.05873, 2026.
10. K. He, B. Chen, Y. Ding, F. Li, C. Teng, D. Ji. *PaSE: Prototype-aligned calibration and Shapley-based equilibrium for multimodal sentiment analysis.* AAAI, 2026 (arXiv:2511.17585).
11. Z. Xie, Y. Yang, J. Wang, X. Liu, X. Li. *Trustworthy multimodal fusion for sentiment analysis in ordinal sentiment space (TMSON).* IEEE TCSVT, 2024 (arXiv:2404.08923).
12. Y. Liu, S. Wang. *FINE: Factorized multimodal sentiment analysis via mutual information estimation.* arXiv:2511.20167, 2025.
13. C. Meng, J. Luo, Z. Yan, Z. Yu, R. Fu, Z. Gan, C. Ouyang. *Tri-Subspace Disentanglement for multimodal sentiment analysis (TSD).* CVPR, 2026 (arXiv:2602.19585).
14. P. Wang, Q. Zhou, Y. Wu, T. Chen, J. Hu. *DLF: Disentangled-Language-Focused multimodal sentiment analysis.* AAAI, 2025 (arXiv:2412.12225).
15. X. Li, X. Zhang, D. Miao, X. Cheng, D. Li, H. Han, Z. Li. *Mixture of disentangled experts with missing modalities for robust multimodal sentiment analysis (DERL).* arXiv:2602.01833, 2026.
16. F. Lv, J. Hu, B. Kang, et al. *DCAF: Dual-branch contrastive adaptive fusion for multimodal sentiment analysis.* Neurocomputing, 2026.
17. Y. Zhou, et al. *DCCF: Decoupling consistency and complementarity for multimodal sentiment analysis.* arXiv:2512.20670, 2025.
