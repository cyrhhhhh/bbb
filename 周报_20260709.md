# 周报
## 一、本周完成的工作
本周基本将代码重构完成，并进行项目的改进，由于之间模块比较杂乱，把 5 个并行创新点收敛成**一个可讲清楚的核心机制**；同步改造代码，保证可编译、接口兼容、无残留引用；
确定的核心机制为 **PCOT（极性耦合最优传输，Polarity-Coupled Optimal Transport）**：把**跨模态对齐**、**一致/冲突分解**、**分歧度量**三件事统一到**同一次最优传输**里完成。

### 1. 模型架构重构（`dmrl.py`）

**核心机制细节：**

本周对模型主体 `dmrl.py` 做了系统性重构，整个流程围绕 PCOT 这一条主线展开，各环节说明如下。

**稀疏极性证据提取。** 过去每个模态会被压成一个笼统的向量，模态内部"哪几处线索真正决定情感"被抹平了。现在改为从每个模态里只挑出少量（4 个）关键的"证据槽"，让模型把注意力集中在最有代表性的几处线索上。每个证据槽只保留两个直观、可解释的属性：一个是**极性**，表示这处线索倾向于正面还是负面情感；另一个是**可靠性**，表示这处线索有多值得信任。最终按可靠性的高低对各证据槽做加权，得到该模态的整体表示——越可信的线索在汇总时占的比重越大。相比之前给每个槽附带"强度、不确定性、时间位置"等一堆属性，现在的表示更精简，也更容易解释。

**极性耦合的跨模态匹配。** 这是本方法与以往工作的最大区别。以往做跨模态对齐时，往往只看两处线索"语义上像不像"。但在情感分析里，语义相近未必情感一致——比如一句话字面积极，语气却是讽刺。为此，我们在衡量两处线索该不该被匹配到一起时，同时考虑两点：一是它们语义上是否接近，二是它们的情感极性是否一致。于是"语义相似但情感相反"的两处线索会被判定为高代价、不容易被硬凑到一起，从根源上把"字面一致"和"情感一致"区分开来。

**求解一张软匹配计划。** 在上述代价基础上，用最优传输（熵正则 Sinkhorn）求解出一张跨模态的"软匹配计划"——它不是非黑即白地判定谁配谁，而是给出各证据槽之间的匹配强度分布。求解过程放在对数域中做迭代归一化，数值稳定、不易溢出。这张匹配计划是后续所有环节的共同基础，不再另起炉灶。

**一致流与冲突流的分离。** 关键创新在于：我们不额外训练任何分类器或子空间来判断"一致还是冲突"，而是直接在**同一张匹配计划**上做拆分。对每一对被匹配到的线索，看它们的情感极性是否同向：同向的部分归入**一致流**（代表两个模态相互印证的证据），反向的部分归入**冲突流**（代表两个模态彼此矛盾的证据）。这样"对齐"和"一致/冲突判定"就由一个机制一次性完成，避免了以往两套独立机制带来的组件冗余和调参负担。

**闭式的分歧分数。** 顺着上一步，一致流和冲突流各自承载了多少"匹配质量"是可以直接算出来的。用冲突流所占的比例，就得到一个介于 0 到 1 之间的**分歧分数**：它衡量这条样本里文本与非语言线索到底有多不一致。这个分数是逐样本、闭式计算出来的，既不需要额外的网络，也不需要人工标注，还能直接拿来当作一个可解释的诊断指标（分数高就说明这条样本模态间冲突大）。

**分歧感知的融合。** 最后，用上面的分歧分数来动态调节一致表示和冲突表示的融合比例：当分歧小时，模型更多地采信相互印证的一致证据；当分歧大时，模型转而正视相互矛盾的冲突证据，而不是简单地把两者平均掉。这样融合权重不再是一个黑箱的注意力分数，而是由一个有明确含义的分歧量来驱动，既提升了对冲突样本的处理能力，也让整个融合过程更透明。

### 2. 损失函数精简（8 项 → 4 项）

| 保留 | 说明 |
| --- | --- |
| 主回归损失 | $L_1$ 回归 |
| 序数分布损失 | 高斯平滑软标签，强化序数结构、提升细粒度 Acc-7 |
| 极性接地损失 | 仅约束**聚合极性**符号与标签一致（保留冲突建模能力） |
| 传输代价损失 | 促成低代价、稳定的匹配 |

移除：单模态回归、角色回归、稀疏正则、反事实路由、方差校准 5 项。


## 结论

- 将"对齐 + 一致/冲突分解 + 分歧度量"统一到**同一次最优传输**，用**单一机制**取代原多分支设计——创新点聚焦、叙事清晰。
- 一致/冲突表示**直接来自传输计划的质量分布**，而非独立分类器/子空间解码，形成与 DCAF/TSD/DCCF/PaSE 的差异化"护城河"。



