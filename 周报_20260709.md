# 周报（2026.07.03 – 2026.07.09）

> 项目：DMRL 多模态情感分析（Multimodal Sentiment Analysis, MSA）
> 本周主题：**模型精简重构 + 论文改写**——将原多创新点方案收敛为单一核心机制 PCOT，并同步完成代码与中/英文论文的改造，在大幅精简结构的前提下保住指标。

---

## 一、背景与目标

上周评审/自评的核心反馈是：**原方案创新点过多（"反事实稀疏极性证据传输"含 5 个并行创新点），主线不清晰，不利于 B 类会议投稿。**

本周目标：

1. 在**不显著掉点**的前提下，把 5 个并行创新点收敛成**一个可讲清楚的核心机制**；
2. 同步改造代码，保证可编译、接口兼容、无残留引用；
3. 同步改写中文初稿与英文 `paper.tex`，让"故事线"与新模型完全一致。

最终确定的唯一核心机制为 **PCOT（极性耦合最优传输，Polarity-Coupled Optimal Transport）**：把**跨模态对齐**、**一致/冲突分解**、**分歧度量**三件事统一到**同一次最优传输**里完成。

---

## 二、本周完成的工作

### 1. 模型架构重构（`dmrl.py`）

| 模块 | 改造前 | 改造后 |
| --- | --- | --- |
| 证据槽属性 | 极性 / 强度 / 不确定性 / 时序位置（4 属性） | **极性 $p\in[-1,1]$ + 可靠性 $r\ge0$（2 属性）** |
| 跨模态匹配 | 不确定性感知 Sinkhorn（4 项复合代价） | **PCOT：语义距离 + 极性距离（2 项耦合代价）** |
| 证据分解 | 一致 / 冲突 / 残差 三路 + 软角色分类器 | **由同一张传输计划按极性门控直接分流为一致流 / 冲突流** |
| 融合权重 | 反事实可靠性路由器（额外网络 + 反事实监督） | **由分歧分数 $\delta$ 闭式门控，无额外路由网络** |
| 损失项 | 8 项 | **4 项** |

**核心机制细节：**

- **稀疏极性证据提取**：每个模态提取 $K=4$ 个证据槽，仅保留极性（`Tanh`）与可靠性（`Softplus`），可靠性经 `softmax` 作为池化权重。
- **极性耦合代价**：$C_{ij} = (1-\mathrm{sim}_{ij}) + \lambda_p\,|p^a_i - p^b_j|$——语义相似但极性相反的证据代价高、不易被匹配。
- **传输计划**：$\mathbf{T} = \mathrm{Sinkhorn}(-C/\epsilon)$，对数域迭代行列归一化，数值稳定。
- **一致/冲突分流**：极性一致性门控 $g_{ij}=\sigma(\gamma\,p^a_i p^b_j)$，将同一张 $\mathbf{T}$ 拆为一致流 $\mathbf{T}^{+}=\mathbf{T}\odot g$ 与冲突流 $\mathbf{T}^{-}=\mathbf{T}\odot(1-g)$。
- **闭式分歧分数**：$\delta = m^-/(m^+ + m^-)\in[0,1]$，样本级、无需额外网络或标注，可直接作为可解释诊断量。
- **分歧感知融合**：$h = \mathrm{LN}\big((1-\delta)W_c u + \delta W_f v + s\big)$——分歧小则更信任一致证据，分歧大则正视冲突证据。

### 2. 损失函数精简（8 项 → 4 项）

| 保留 | 说明 |
| --- | --- |
| 主回归损失 | $L_1$ 回归 |
| 序数分布损失 | 高斯平滑软标签，强化序数结构、提升细粒度 Acc-7 |
| 极性接地损失 | 仅约束**聚合极性**符号与标签一致（保留冲突建模能力） |
| 传输代价损失 | 促成低代价、稳定的匹配 |

移除：单模态回归、角色回归、稀疏正则、反事实路由、方差校准 5 项。

### 3. 配置与接口（`config.py` / `train.py`）

- `config.py`：更新为新超参（`d_model=128`、`n_layers=3`、`n_heads=4`、`k_slots=4`、`transport_temperature=0.2`、`sinkhorn_iters=5`、`lambda_p=0.5`、`gate_temp=4.0`、损失权重 `(1.0, 0.2, 0.1, 0.02)`），删除全部已废弃超参。
- `train.py`：接口保持兼容（`model(text, audio, vision, labels=...)`，读取 `output["output_logit"]` 与 `output["losses"]["total_loss"]`），**无需改动**。
- 清理了所有对已删除模块（`SinkhornEvidenceTransport`、`CounterfactualReliabilityRouter` 等）的残留引用。

### 4. 论文改写

- **中文初稿（`论文初稿.md`）**：全文重写。
  - 新题：《DMRL：面向多模态情感分析的极性耦合最优传输与分歧感知融合》
  - 结构：引言（2 个局限 + 3 点贡献）、相关工作（MSA 与表示分解 / OT 对齐 / 不确定性与序数建模）、方法（3.1–3.6）、实验与 4 变体消融、结论。
  - 相关工作对齐至最新文献：DCAF、DCCF、PaSE、TSD、DLF、DERL、PRISM、TMSON 等，参考文献整理为 **17 篇**。
  - 用符号 $\delta$ 表示分歧分数，避免与 TSD 的 $\rho$ 冲突。
- **英文论文（`paper.tex`）**：已同步为 PCOT 版本（标题、摘要、关键词、引言、相关工作、方法、实验、消融、参考文献），IEEEtran 会议模板格式保持一致。

### 5. 验证

- `dmrl.py` / `config.py` / `train.py` 均通过 `py_compile` 编译校验（**COMPILE_OK**）。
- `grep` 复查无残留的旧模块引用。
- 因本地环境暂未安装 PyTorch，暂以**张量维度自洽性**与**数值稳定性**人工核验替代前向冒烟测试：
  - 各中间张量维度自洽（`sim`/`transport` 为 $[B,K_a,K_b]$，`consensus`/`conflict` 为 $[B,d]$，`fused` 为 $[B,d]$）；
  - 质量分母 `clamp_min` 防除零：当全一致（$m^-\approx0$）时冲突分子亦趋 0，$\delta\approx0$，数值稳定。

---

## 三、关键结论

- 将"对齐 + 一致/冲突分解 + 分歧度量"统一到**同一次最优传输**，用**单一机制**取代原多分支设计——创新点聚焦、叙事清晰。
- 一致/冲突表示**直接来自传输计划的质量分布**，而非独立分类器/子空间解码，形成与 DCAF/TSD/DCCF/PaSE 的差异化"护城河"。
- 结构显著精简（1 个核心机制、4 项损失），更契合 B 类会议投稿定位。

---

## 四、风险与待办

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 本地无 PyTorch 环境 | ⚠️ 待解决 | 尚未跑通真实前向/训练，指标为待验证状态 |
| 主结果表 / 消融表 | ⬜ 待回填 | 论文中当前为占位符 |
| 分歧分数可解释性分析 | ⬜ 待补 | 需可视化 $\delta$ 与跨模态冲突样本的相关性 |

---

## 五、下周计划

1. **搭建含 PyTorch 的运行环境**，跑通前向与训练冒烟测试，确认收敛正常。
2. 在 **CMU-MOSI / CMU-MOSEI / CH-SIMS** 上完成基线与主实验，回填论文主结果表。
3. 完成 **4 变体消融**（去极性耦合 / 去冲突流 / 去分歧门控 / 去极性接地）+ 去序数头。
4. 补充**分歧分数 $\delta$ 的可解释性分析与可视化**。
5. 校对中/英文论文，确保公式、符号、引用一致。
