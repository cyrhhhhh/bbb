# DMRL 优化技术方案：单一核心创新点（面向 SCI）

> 目标：把原来的 5 个并列创新点，收敛为**一个**足够新颖、自洽、可解释、非冗余的核心机制，
> 使方法聚焦、消融干净、故事完整，达到合格 SCI 论文水平，同时保住精度。

---

## 0. 一句话贡献

我们提出 **极性耦合最优传输（Polarity-Coupled Optimal Transport, PCOT）**：
一个将**语义距离**与**情感极性距离**耦合进同一传输代价的最优传输公式；
求解得到的**单一传输计划** $\mathbf{T}$ 被沿极性一致性拆分为**一致流** $\mathbf{T}^{+}$ 与**冲突流** $\mathbf{T}^{-}$，
由此得到一个闭式的、样本级的**跨模态分歧度** $\rho$，并以 $\rho$ 引导融合与置信度。

> 这是**一个**机制：对齐、一致/冲突分解、分歧度量，三者都由同一个 $\mathbf{T}$ 导出，
> 而非"OT + 角色分解 + 路由 + 序数头"的模块堆叠。

**建议新标题**：《DMRL：面向多模态情感分析的极性耦合最优传输与分歧感知融合》
（DMRL 可重新诠释为 **D**isagreement-aware **M**ultimodal **R**epresentation **L**earning）

---

## 1. 动机与核心洞察

在 MSA 中，非语言线索常常**确认、补充或反转**文本的表面极性。现有融合（注意力 / OT / 原型）
主要度量**语义**对应关系然后池化，**没有显式量化**"相互对应的跨模态线索在情感极性上是否一致"，
更没有把这种（不）一致性用于驱动融合。

**核心洞察**：如果把极性距离耦合进 OT 代价，那么最优传输计划 $\mathbf{T}$ 不仅完成语义对齐，
其质量分布本身就编码了"哪些跨模态线索一致、哪些冲突"。于是**一个** $\mathbf{T}$ 同时给出：
(i) 对齐，(ii) 一致/冲突分解，(iii) 一个可解释的分歧标量 $\rho$。

---

## 2. 与现有工作的区分（SCI 新颖性定位）

| 对比对象 | 它们怎么做 | PCOT 的关键不同 |
| --- | --- | --- |
| **PaSE**（OT + Shapley）| OT 仅用**特征距离**做对齐/校准，未从传输计划导出情感一致/冲突 | 把**极性耦合进代价**，并**分解传输计划**为一致/冲突子流——一个全新的对象 |
| **PRISM / MCAF**（互补/冲突/冗余）| 用原型或注意力**隐式分类**交互状态，难解释 | 用形式化 OT 耦合与显式代价**推导**一致/冲突，闭式给出分歧度 $\rho$ |
| **MISA / DLF / TSD**（共享/私有解耦）| 沿**静态**的共享/私有轴逐模态解耦 | 沿**样本相关、跨模态**的极性一致性轴解耦，由传输定义 |

结论：PCOT 是一个**界定清晰、机制新颖、可解释**的单一贡献，与最接近的工作有明确区分，
非"清单式"堆叠——正是 SCI 期望的"足够但不冗余"。

---

## 3. 重构后的方法流程（最小化）

记模态 $m\in\{t,a,v\}$。文本经 BERT，声/视经卷积 stem，统一由共享 Transformer 编码得到序列 $\mathbf{H}_m$。

### 3.1 证据词元提取（支撑组件，不作为创新点）

对每个模态用槽注意力提取 $K$ 个紧凑证据词元 $\{\mathbf{z}^m_i\}_{i=1}^{K}$，
每个词元仅附带**一个标量极性** $p^m_i=\tanh(\cdot)\in[-1,1]$。
（**删除**原先的 strength / uncertainty / 位置 三个头——它们对核心机制冗余。）

### 3.2 极性耦合最优传输 PCOT（核心）

对每个模态对 $(a,b)\in\{(t,a),(t,v)\}$：

**(1) 耦合代价**（语义 + 极性）：
$$
C_{ij} \;=\; \underbrace{\big(1-\cos(\mathbf{z}^a_i,\mathbf{z}^b_j)\big)}_{\text{语义距离}} \;+\; \lambda_p\,\underbrace{|p^a_i - p^b_j|}_{\text{极性距离}}.
$$

**(2) 熵正则最优传输**（Sinkhorn，均匀边缘）：
$$
\mathbf{T} \;=\; \arg\min_{\mathbf{T}\in\mathcal{U}} \;\langle \mathbf{T}, C\rangle - \epsilon\,H(\mathbf{T}) \;=\; \mathrm{Sinkhorn}(-C/\epsilon).
$$

**(3) 极性一致性门控**（平滑，$\kappa$ 为温度）：
$$
\alpha_{ij} \;=\; \sigma\!\big(\kappa\, p^a_i\,p^b_j\big)\in(0,1),
\qquad
\begin{cases}
\alpha_{ij}\!\to\!1 & \text{同号（一致）}\\
\alpha_{ij}\!\to\!0 & \text{异号（冲突）}
\end{cases}
$$

**(4) 分解同一传输计划**为一致/冲突子流：
$$
\mathbf{T}^{+} = \mathbf{T}\odot\alpha,\qquad \mathbf{T}^{-} = \mathbf{T}\odot(1-\alpha).
$$

**(5) 角色证据**（一致→平均增强；冲突→带符号差异）：
$$
\mathbf{c} = \frac{\sum_{ij} T^{+}_{ij}\cdot\tfrac12(\mathbf{z}^a_i+\mathbf{z}^b_j)}{\sum_{ij}T^{+}_{ij}+\varepsilon},
\qquad
\mathbf{f} = \frac{\sum_{ij} T^{-}_{ij}\cdot(\mathbf{z}^a_i-\mathbf{z}^b_j)}{\sum_{ij}T^{-}_{ij}+\varepsilon}.
$$

**(6) 跨模态分歧度**（闭式、可解释、样本级）：
$$
\rho \;=\; \frac{\sum_{ij}T^{-}_{ij}}{\sum_{ij}T^{+}_{ij}+\sum_{ij}T^{-}_{ij}+\varepsilon}\in[0,1].
$$

两个模态对的 $\mathbf{c},\mathbf{f}$ 分别聚合（求和后投影），$\rho$ 取平均。

### 3.3 分歧感知融合（核心机制的下游使用，属于同一创新点）

分歧度 $\rho$ 直接充当门控：**分歧低→信任一致证据；分歧高→启用冲突证据并回落到单模态锚点**：
$$
\mathbf{h} \;=\; \mathrm{LN}\!\Big( (1-\rho)\,\mathbf{W}_c\,\bar{\mathbf{c}} \;+\; \rho\,\mathbf{W}_f\,\bar{\mathbf{f}} \;+\; \mathbf{W}_z\,[\bar{\mathbf{z}}_t;\bar{\mathbf{z}}_a;\bar{\mathbf{z}}_v]\Big),
$$
其中 $\bar{\mathbf{z}}_m$ 为模态证据的均值池化。预测由标准回归头给出 $\hat{y}=\mathrm{Head}(\mathbf{h})$。

> **注**：可选保留一个序数分布头替代标量回归头以提升 Acc-7，但**仅作为实现细节**，不作为创新点。

---

## 4. 训练目标（精简为 3~4 项，全部服务于核心）

| 损失 | 定义 | 作用 |
| --- | --- | --- |
| $\mathcal{L}_{\text{main}}$ | $|\hat{y}-y|$ | 主回归 |
| $\mathcal{L}_{\text{pol}}$ | $\mathrm{BCE}\big(\sigma(\gamma\bar{P}),\,\mathbb{1}[y>0]\big)$，$\bar P$ 为聚合极性 | **极性接地**：让 $p$ 有情感语义，支撑一致/冲突分解 |
| $\mathcal{L}_{\text{trans}}$ | $\langle\mathbf{T},C\rangle$（小权重）| 让传输计划贴合代价（可选） |
| $\mathcal{L}_{\text{ord}}$ | 软序数交叉熵（可选）| 保 Acc-7（实现细节） |

$$
\mathcal{L}=\mathcal{L}_{\text{main}}+\lambda_{\text{pol}}\mathcal{L}_{\text{pol}}+\lambda_{t}\mathcal{L}_{\text{trans}}+\lambda_{\text{ord}}\mathcal{L}_{\text{ord}}.
$$

> $\mathcal{L}_{\text{pol}}$ 是**关键的支撑损失**：整个一致/冲突分解依赖极性 $p$ 有意义，因此需要弱监督接地。
> 它服务于核心机制，不构成新的创新点。

---

## 5. 消融设计（全部围绕唯一创新点，干净）

| 变体 | 操作 | 检验 |
| --- | --- | --- |
| w/o 极性耦合代价 | $\lambda_p=0$ | 极性是否改善**对齐** |
| w/o 冲突流 | 只用 $\bar{\mathbf{c}}$ | 冲突建模的价值 |
| w/o 分歧门控 | $\rho\!\to\!0.5$ 固定 | $\rho$ 引导融合的价值 |
| w/o 极性接地 | $\lambda_{\text{pol}}=0$ | 极性监督的必要性 |

四个消融点全部指向 PCOT 的不同侧面，形成聚焦、自洽的实验故事。

---

## 6. 可解释性分析（SCI 加分）

- **分歧度 $\rho$ 与难例的相关性**：统计 $\rho$ 高的样本是否更可能是"文本正、语音/表情负"的冲突样本；
- **传输可视化**：展示某句话的一致流 vs 冲突流，指出跨模态在何处一致/冲突；
- **$\rho$ 与误差/置信度**：$\rho$ 越高预测越不确定，可支撑选择性预测。

---

## 7. 相对现有代码/论文的改动映射

### 7.1 代码（dmrl.py / config.py）
- `SparsePolarEvidenceExtractor`：删 strength/uncertainty/position 头，只留 evidence + polarity。
- `SinkhornEvidenceTransport`：代价改为"语义 + 极性"两项；**新增** $\alpha$ 门控与 $\mathbf{T}^{+}/\mathbf{T}^{-}$ 拆分、$\rho$ 计算。
- `SoftRoleEvidenceTransport` → 精简为 **PCOT 分解**（产出 $\bar{\mathbf{c}},\bar{\mathbf{f}},\rho$），删残差角色与双向复杂逻辑。
- **删除** `CounterfactualReliabilityRouter` 及 `_counterfactual_preds`（6 次额外前向、KL 损失全部移除）。
- 融合改为 $\rho$ 门控（3.3 式）；`OrdinalDistributionHead` 保留为可选头。
- `DMRLLoss`：8 项 → 3~4 项（main + pol + 可选 trans/ord）；删 cf、var、uni、role、sparse 等。
- `config.py`：删 `w_cf,w_var,cf_tau,use_av_pair,topk_attn` 等无关项，新增 `lambda_p,kappa,lambda_pol`。
- `train.py`：**接口不变**，无需改动。

### 7.2 中文论文（论文初稿.md）
标题/摘要/关键词/贡献（→**1 条核心 + 简述**）/方法（3.2–3.6 → PCOT 单节）/
相关工作（2.3 反事实删、2.4 序数压缩）/损失（→ 3~4 项）/消融（→ 4 个 PCOT 变体）。

### 7.3 英文论文（paper.tex）
与中文一一对应同步。

---

## 8. 精度保障（为什么不掉）

1. **信息不丢**：融合同时聚合一致证据 $\bar{\mathbf{c}}$、冲突证据 $\bar{\mathbf{f}}$ 与单模态锚点 $\bar{\mathbf{z}}$，无信息损失。
2. **极性接地** $\mathcal{L}_{\text{pol}}$ 提供弱监督，通常**有助**收敛与判别。
3. **可选序数头**保留 → Acc-7 不受影响。
4. **删的都是脆弱/低收益项**（反事实多次前向、方差校准），去掉后训练更快更稳。
5. **兜底**：若个别指标下降，优先加回 $\mathcal{L}_{\text{trans}}$ 或 $\mathcal{L}_{\text{ord}}$ 小权重，而非恢复被删的旧模块。

---

## 9. 精简前后对比

| 项目 | 原方案 | PCOT 方案 |
| --- | --- | --- |
| 并列创新点 | 5 | **1（PCOT）** |
| 证据属性 | 4 | **1（极性）** |
| 传输代价项 | 4 | **2（语义+极性）** |
| 融合/路由 | 反事实多次前向 + KL | **$\rho$ 门控（闭式）** |
| 损失项 | 8 | **3~4** |
| 每步前向 | 7 | **1** |

---

## 10. 待你确认

1. 是否采用 **PCOT** 作为唯一核心创新点？
2. 一致/冲突用**二分**（推荐，更简洁）还是保留"残差"做三分？
3. 是否保留**可选序数头**（保 Acc-7）与 $\mathcal{L}_{\text{trans}}$ 小权重？

确认后，我按第 7 节顺序落地到**代码 + 中文论文 + 英文论文**。
