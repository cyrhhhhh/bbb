# DMRL 精简方案说明（面向 B 类会议）

> 目标：把当前"5 个并列创新点"收敛为**聚焦、可复现、故事清晰**的投稿版本，
> 同时**保住精度**。本文件只是方案，暂不改动代码与论文；确认方向后再执行。

---

## 0. 一句话结论

**把 5 个并列卖点，重构为「1 个核心贡献 + 2 个支撑设计」。**
真正脆弱/低收益的模块（反事实路由的多次前向监督）从代码删除；
能提分的组件（序数头）**保留但降级为实现细节**，不再当创新点包装。
这样叙事聚焦、训练更稳更快，且精度不掉。

---

## 1. 核心原则：区分「创新点包装」与「代码模块」

| 维度 | 说明 |
| --- | --- |
| **创新点数量** | 给审稿人看的"故事线"。太多 → 不聚焦、被质疑拼凑。**必须砍到 1~2 个。** |
| **代码模块** | 拿分的工具。有效的模块即使不当卖点，也可保留成"实现细节"。 |

> 结论：精简 = **重组叙事** + **删冗余/脆弱** + **保留有效组件**。不是无脑删代码。

---

## 2. 当前 5 个创新点的处置决策（推荐：方案 A）

**核心主线一句话：稀疏证据 → 最优传输对齐 → 一致/冲突/残差角色分解。**

| # | 创新点 | 对应模块 | 处置 | 理由 |
| --- | --- | --- | --- | --- |
| 1 | 稀疏极性证据槽（4 属性）| `SparsePolarEvidenceExtractor` | **简化保留** | 是 #2/#3 的输入，必须留；但砍冗余属性 |
| 2 | Sinkhorn 证据传输 | `SinkhornEvidenceTransport` | **保留（核心）** | 技术深度最高、卖点最强 |
| 3 | 一致/冲突/残差角色分解 | `SoftRoleEvidenceTransport` | **保留（核心）** | 与 #2 连贯，构成完整故事 |
| 4 | 反事实可靠性路由 | `CounterfactualReliabilityRouter` | **删卖点 + 换轻量路由** | 多次前向、组合爆炸、训练脆弱 |
| 5 | 序数分布预测头 | `OrdinalDistributionHead` | **降级为实现细节** | 增量小（TMSON 已做），但保 Acc-7 |

**备选方向（若你不认同 A）：**
- **方案 B（2 个核心）**：#2+#3 主线 + #4 反事实路由作为"一个亮点子模块"，删 #5。适中，但保留 #4 会带回训练脆弱与消融复杂度。
- **方案 C（以 #4 为核心）**：保留反事实路由，OT 换成简单注意力对齐。风险最高，不推荐。

---

## 3. 代码改动清单（dmrl.py / config.py / train.py）

> 原则：**主干（编码→证据→OT→角色→预测）完整保留**，只动"辅助属性 / 脆弱监督 / 小权重损失"。

### 3.1 `SparsePolarEvidenceExtractor` —— 属性 4→2，预测头 4→2

- **保留**：`evidence`（证据向量）、`polarity`（角色分解必需）。
- **合并**：把 `strength` 与 `uncertainty` 两个头，合并为**单个 `reliability` 头**（直接输出可靠性标量，用于 `slot_weight` 池化）。
- **简化**：`tau`（时序位置）只用注意力加权位置 `attn_position`，**删掉 `position_head`（learned_position）**。
- **删除**：独立的 `uncertainty` 输出（连带下游对 `uncertainty` 的所有依赖，见 3.2/3.4）。

影响：参数/头数减少，`evidence` 主向量不变 → **对精度影响极小**。

### 3.2 `SinkhornEvidenceTransport` —— 复合代价 4 项→3 项

- 代价从 `feat + time + polar + unc` → **`feat + time + polar`**（删 `cost_unc`）。
- `sinkhorn()` 迭代逻辑保留不变。

影响：`unc` 项权重本就最小（`lambda_u=0.25`），删除后传输仍由"语义+时序+极性"主导，
正好强化"一致/冲突"故事 → **精度基本不变**。

### 3.3 `SoftRoleEvidenceTransport` / `ConsensusConflictResidualExperts` —— **原样保留**

这是核心，不动。仅 `_pair_roles` 里删除对 `paired_unc_b / pair_unc` 的引用（随 3.1 的 uncertainty 移除）。
角色分数改为：`cons = avg_sim - λ·gap`，`conf = avg_sim + λ·gap`，`res = -avg_sim`（去掉不确定性项）。

### 3.4 `CounterfactualReliabilityRouter` → 替换为轻量 `ReliabilityRouter`

- **删除**：`counterfactual_targets()`、`_counterfactual_preds()`（6 次额外前向）、KL 反事实损失。
- **替换为**：用 `[pooled_t; pooled_a; pooled_v; reliability]` 直接 MLP→softmax 得模态权重；
  角色权重同理用 `[cons; conf; res]`→MLP→softmax。即**保留"可靠性加权路由"，去掉"反事实监督"**。

影响：融合主干不变，只是路由权重来源从"反事实敏感度"变为"可靠性表示"。
**回归精度通常持平或更稳**，且训练更快（少 6 次前向）、更少 OOM 风险。

### 3.5 `OrdinalDistributionHead` —— 保留代码，降级叙事

- **代码保留**（继续输出 `output_logit` = 锚点期望），以**保住 Acc-7**。
- `predictive_variance` 可保留为可选输出，但**从损失里移除方差校准项**（见 3.6）。
- 论文中不再称其为"创新点"，仅作为"预测头实现细节"一句话带过。

### 3.6 `DMRLLoss` —— 8 项→4~5 项

| 损失项 | 现权重 | 处置 |
| --- | --- | --- |
| `main_loss`（L1） | 1.0 | **保留** |
| `ordinal_loss`（软序数 CE） | 0.2 | **保留**（保 Acc-7）|
| `role_loss`（角色深监督） | 0.1 | **保留**（支撑角色分解）|
| `sparse_loss`（稀疏正则） | 0.01 | **保留**（支撑"稀疏证据"卖点）|
| `transport_loss`（⟨T,C⟩）| 0.02 | **可选保留**（小权重，强化 OT 故事）|
| `uni_loss`（单模态深监督）| 0.2 | **可选保留**（助模态平衡）→ 建议保留 |
| `counterfactual_loss`（KL）| 0.05 | **删除**（随 #4）|
| `variance_loss`（\|Var−err²\|）| 0.005 | **删除**（权重极小，近乎无效）|

- **推荐损失集合（5 项）**：`main + ord + role + sparse + uni`
- **最小损失集合（4 项）**：`main + ord + role + sparse`

影响：删掉的 `cf`(0.05) 与 `var`(0.005) 权重小、且是脆弱/无效项 → **精度不掉，训练更稳**。

### 3.7 `config.py` / `train.py`

- `config.py`：删除 `w_cf`、`w_var`、`cf_tau`、（可选）`w_transport`；保留其余。
- `train.py`：**无需改动**（读的是 `output["losses"]["total_loss"]` 与 `output["output_logit"]`，接口不变）。

---

## 4. 中文论文改动清单（论文初稿.md）

| 位置 | 改动 |
| --- | --- |
| **标题** | 去掉 "反事实"，如 →《DMRL：稀疏证据最优传输与角色分解的多模态情感分析》|
| **摘要** | 删"反事实路由 / 序数分布 / 校准方差"相关句；聚焦"稀疏证据 + OT 对齐 + 一致/冲突/残差分解" |
| **关键词** | 删 "反事实推理"、"不确定性估计"；保留"最优传输、证据分解" |
| **引言 3 个局限** | 第 3 条（因果依据）弱化，改为"融合权重缺乏对证据角色的显式建模" |
| **贡献列表** | 4 条 → **2 条**（稀疏证据的 OT 对齐；一致/冲突/残差角色分解 + 可靠性加权融合）|
| **2.3 反事实/因果** | **大幅压缩或整节删除**（不再是卖点）|
| **2.4 不确定性/序数** | 压缩为 1~2 句（序数头仅作实现细节）|
| **3.5 反事实路由** | 改写为简短的"**可靠性路由**"，删反事实监督公式 |
| **3.6 序数头** | 并入 3.5 预测，弱化为"预测头"一段 |
| **3.7 训练目标** | 损失公式 8 项 → 4~5 项 |
| **4.4 消融** | 变体 5 个 → **3 个**：w/o 复合代价、w/o 角色分解、w/o 稀疏正则 |
| **4.5 分析** | 删"反事实路由一致性"分析；保留证据可解释性 |

---

## 5. 英文论文改动清单（paper.tex）

与中文版**一一对应**同步：
- `\title`：去掉 "Counterfactual"。
- `abstract`：删 counterfactual router / ordinal distribution / calibrated variance 相关句。
- `IEEEkeywords`：删 `counterfactual reasoning`、`uncertainty estimation`。
- Introduction 三点动机、Contributions（4→2）、Related Work（2.3/2.4 压缩）、
  Method（3.5 改可靠性路由、3.6 弱化）、Loss（8→4~5）、Ablation（5→3）。

---

## 6. 精度保障策略（为什么不会掉）

1. **主干完整**：编码器、稀疏证据、OT 传输、角色分解、序数预测头**全部保留**。
2. **序数头保留** → Acc-7 不受影响。
3. **删的都是"辅助/脆弱/小权重"项**：`cf`(0.05)、`var`(0.005)、`cost_unc`(0.25)、learned_position。
4. **反事实路由 → 可靠性加权**：融合主干不变，路由更稳定，回归精度通常持平或略升。
5. **验证兜底**：精简版 vs 原版各跑 3 个种子对比 `MAE / Acc-2 / Acc-7`，确认不掉再定稿。
   （若个别指标掉，优先恢复 `uni_loss` 或 `transport_loss` 小权重，而非恢复 #4/#5。）

---

## 7. 精简前后对比

| 项目 | 精简前 | 精简后 |
| --- | --- | --- |
| 并列创新点（叙事）| 5 | **2** |
| 证据槽属性 | 4（极性/强度/不确定性/位置）| **2（极性/可靠性）** |
| 传输代价项 | 4 | **3** |
| 路由方式 | 反事实多次前向 + KL | **轻量可靠性加权** |
| 损失项 | 8 | **4~5** |
| 每步前向次数 | 1 + 6 次反事实 | **1** |
| 序数头 | 创新点 | **实现细节（保留）** |

---

## 8. 执行计划（确认方向后我来做）

1. **改代码**：`SparsePolarEvidenceExtractor`(属性精简) → `SinkhornEvidenceTransport`(代价 3 项)
   → 替换 `CounterfactualReliabilityRouter` 为 `ReliabilityRouter` → 精简 `DMRLLoss` → 同步 `config.py`。
2. **改中文论文**：标题/摘要/贡献/方法/相关工作/损失/消融。
3. **改英文论文**：与中文一一对应同步。
4. **跑通验证**：确认前向无误、损失可回传；如有数据，多种子对比精度。

---

## 9. 待你确认

- 是否采用**方案 A**（推荐）？还是 B / C？
- 损失用**推荐 5 项**还是**最小 4 项**？
- `transport_loss`、`predictive_variance` 是否保留为可选输出？

确认后我按第 8 节顺序执行。
